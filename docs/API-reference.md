# The Firn API

Generated by `scripts/gen_api_docs.py` from what the program reports;
do not edit by hand. Every action below is the same code the matching
menu item runs, so this manual and the program cannot disagree.

Three ways to call these, all the same surface: `firn-cli do <name> '<json>'`,
a line on the driver socket (`do <name> <json>`), or a `.PspScript` file.
`docs/API.md` explains the transports; `docs/api.json` is this manual as
machine-readable JSON Schema, one schema per action.

Replies are JSON. Success is `{"ok":true}` and whatever the action returns;
a refusal comes back as a message and a non-zero exit from `firn-cli`.

46 actions and 161 inherited command names.

## Documents

### `file.close`

Close the current image, discarding changes.

Takes no parameters.


### `file.new`

Create an image.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `width` | number | no | `800` | pixels |
| `height` | number | no | `600` | pixels |
| `color` | string | no | `white` | #RRGGBB background, or transparent |


### `file.open`

Open an image file.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `path` | string | yes |  | file to open |


### `file.revert`

Load the saved file again, dropping every change.

Takes no parameters.


### `file.save`

Save the image where it came from.

Takes no parameters.


### `file.save_as`

Save the image to a path, format taken from the extension.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `path` | string | yes |  | where to write it |


## Editing

### `edit.clear`

Clear the selection.

Takes no parameters.


### `edit.content_aware_fill`

Rebuild the selection from the rest of the picture.

Takes no parameters.


### `edit.copy`

Copy the active layer through the selection.

Takes no parameters.


### `edit.copy_merged`

Copy the composite through the selection.

Takes no parameters.


### `edit.cut`

Cut the selection.

Takes no parameters.


### `edit.paste_as_image`

Paste the clipboard as a new image.

Takes no parameters.


### `edit.paste_as_layer`

Paste the clipboard as a new layer.

Takes no parameters.


### `edit.paste_into_selection`

Scale the clipboard into the selection.

Takes no parameters.


### `edit.redo`

Redo one step.

Takes no parameters.


### `edit.repeat`

Apply the last adjustment or effect again.

Takes no parameters.


### `edit.undo`

Undo one step.

Takes no parameters.


## The view

### `view.toggle`

Turn a view aid on or off.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `what` | string (rulers, grid, guides, assistants, marquee, snap_guides, snap_grid, snap_assistants) | yes |  | the aid to switch |
| `on` | boolean | no | `the opposite of now` | leave it out to toggle |


### `view.zoom`

Set the zoom, or fit / actual size.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `zoom` | number | no | `1` | 1 is actual size |
| `mode` | string (fit, actual, set) | no | `fit unless zoom is given` | which way to zoom |


### `view.zoom_to_selection`

Fill the window with the selection.

Takes no parameters.


## The whole image

### `image.crop_to_selection`

Crop to the selection.

Takes no parameters.


### `image.flip`

Flip top to bottom.

Takes no parameters.


### `image.mirror`

Mirror left to right.

Takes no parameters.


### `image.resize`

Resize the image.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `width` | number | no |  | pixels |
| `height` | number | no |  | pixels |
| `filter` | string (smart, lanczos, mitchell, bicubic, bilinear, nearest, edge_directed) | no | `smart` | how to resample |


### `image.rotate`

Rotate the image.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `degrees` | number | no | `90` | clockwise |


## Selections

### `select.all`

Select everything.

Takes no parameters.


### `select.ellipse`

Select an ellipse inside a rectangle, in image pixels.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `x0` | number | no |  |  |
| `y0` | number | no |  |  |
| `x1` | number | no |  |  |
| `y1` | number | no |  |  |
| `feather` | number | no | `0` | pixels of soft edge |


### `select.invert`

Invert the selection.

Takes no parameters.


### `select.none`

Drop the selection.

Takes no parameters.


### `select.rect`

Select a rectangle, in image pixels.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `x0` | number | no |  |  |
| `y0` | number | no |  |  |
| `x1` | number | no |  |  |
| `y1` | number | no |  |  |
| `feather` | number | no | `0` | pixels of soft edge |


## Layers

### `layer.arrange`

Move the active layer up or down its group.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `steps` | number | no | `1` | positive is up |


### `layer.delete`

Delete the active layer.

Takes no parameters.


### `layer.duplicate`

Duplicate the active layer.

Takes no parameters.


### `layer.merge`

Merge layers.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `what` | string (down, visible, all) | no | `down` | which layers to merge |


### `layer.move_onto`

Restack a layer where another one sits.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `from` | number | yes |  | index |
| `onto` | number | yes |  | index |


### `layer.new`

Add a raster layer.

Takes no parameters.


### `layer.new_group`

Group the active layer.

Takes no parameters.


### `layer.new_vector`

Add a vector layer.

Takes no parameters.


### `layer.promote_background`

Turn the Background layer into an ordinary one.

Takes no parameters.


### `layer.properties`

Set the active layer's name, opacity, blend mode or visibility.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `name` | string | no |  |  |
| `opacity` | number | no |  | percent, 0 to 100 |
| `blend` | string | no |  | blend mode name, as the palette shows it |
| `visible` | boolean | no |  |  |


### `layer.select`

Make a layer active, by index from the bottom.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `index` | number | yes |  | 0 is the bottom |


## Tools and materials

### `tool.brush_size`

Set the brush size in pixels.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `size` | number | yes |  | pixels, 1 to 500 |


### `tool.color`

Set the foreground or background color.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `color` | string | yes |  | #RRGGBB |
| `which` | string (foreground, background) | no | `foreground` | which material |


### `tool.select`

Choose a tool by the name shown in the palette.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `name` | string | yes |  | the name shown in the tools palette |


## The program itself

### `app.describe`

List every action, tool and option this build offers.

Takes no parameters.


### `app.screenshot`

Write what the window is showing to a PNG.

| parameter | type | required | default | meaning |
|---|---|---|---|---|
| `path` | string | yes |  | file to write |


## Inherited commands

Names from the program this grew out of, taking its parameter names
verbatim (`docs/COMMANDS.md`). They are called the same way as the
actions above. A trailing `*` marks a prefix, so `NewAdjustmentLayer*`
means `NewAdjustmentLayerCurves` and its siblings.

`AddBorders`, `AddGuide`, `AddNoise`, `AdjustPosition`, `AgedNewspaper`, `AutoColorBalance`,
`AutoContrastEnhancement`, `AutoSaturationEnhancement`, `Blinds`, `Blur`, `BlurAverage`, `BlurMore`,
`BrushStrokes`, `Buttonize`, `Chrome`, `Clarify`, `ClearSelection`, `ColorAdjustBrightnessContrast`,
`ColorAdjustChannelMixer`, `ColorAdjustColorBalance`, `ColorAdjustCurves`, `ColorAdjustGammaCorrect`, `ColorAdjustHSL`, `ColorAdjustHueMap`,
`ColorAdjustLevels`, `ColorAdjustThreshold`, `ColorInc16`, `ColorInc256`, `ColoredEdges`, `Colorize`,
`ContentAwareFill`, `Copy`, `CountImageColors`, `Crop`, `CropToSelection`, `Cut`,
`DecreaseColorsTo16`, `DecreaseColorsTo16Million`, `DecreaseColorsTo2`, `DecreaseColorsTo256`, `DecreaseColorsToX`, `DeleteLayer`,
`Despeckle`, `DigitalCameraNoiseRemoval`, `Dilate`, `DropShadow`, `EdgePreservingSmooth`, `Emboss`,
`EnableOptimizedScriptUndo`, `EnhanceEdges`, `EnhanceEdgesMore`, `Erode`, `FadeCorrection`, `FileClose`,
`FileOpen`, `FileSave`, `FileSaveAs`, `Fill`, `FindEdges`, `Flip`,
`FloatSelection`, `GaussianBlur`, `GetMaterial`, `GetRasterSelectionRect`, `GlowingEdges`, `Grayscale`,
`Greyscale`, `Halftone`, `HistogramAdjustment`, `HistogramEqualize`, `HistogramStretch`, `ImageInfo`,
`IncreaseColorsTo16Bit`, `IncreaseColorsTo16Million`, `InnerBevel`, `JPEGArtifactRemoval`, `Kaleidoscope`, `LayerArrange`,
`LayerArrangeMoveDown`, `LayerArrangeMoveIn`, `LayerArrangeMoveOut`, `LayerArrangeMoveUp`, `LayerArrangeToBottom`, `LayerArrangeToTop`,
`LayerArrangeUngroup`, `LayerConvertToRaster`, `LayerDuplicate`, `LayerMergeAll`, `LayerMergeDown`, `LayerMergeVisible`,
`LayerPromoteBackground`, `LayerProperties`, `LayerSetVisibility`, `MaskShowAll`, `Median`, `Mirror`,
`ModifySelection`, `Mosaic`, `MotionBlur`, `Mover`, `MsgBox`, `NegativeImage`,
`NewAdjustmentLayer*`, `NewFile`, `NewLayerGroup`, `NewMaskLayerHide`, `NewMaskLayerShow`, `NewRasterLayer`,
`NewVectorLayer`, `OneStepPhotoFix`, `PageCurl`, `PasteAsNewImage`, `PasteAsNewLayer`, `PasteIntoSelection`,
`Pinch`, `Pixelate`, `Posterize`, `RedoLastUndo`, `Resize`, `ResizeCanvas`,
`ReturnImageInfo`, `ReturnLayerProperties`, `Ripple`, `Rotate`, `SaltAndPepper`, `SavePalette`,
`SelectAll`, `SelectContract`, `SelectDocument`, `SelectExpand`, `SelectFeather`, `SelectInvert`,
`SelectLayer`, `SelectLoadAlpha`, `SelectNone`, `SelectPreviousTool`, `SelectPromote`, `SelectSaveAlpha`,
`SelectSmooth`, `SelectTool`, `Selection`, `Sepia`, `SetMaterial`, `Sharpen`,
`SharpenMore`, `ShowGrid`, `ShowGuides`, `Soften`, `SoftenMore`, `Solarize`,
`Spherize`, `SplitToCMYK`, `SplitToHSL`, `SplitToRGB`, `TextEx`, `Twirl`,
`UndoLastCmd`, `UnsharpMask`, `UserDefinedFilter`, `Wave`, `Weave`

## Tools

Names `tool.select` accepts:

`Pan`, `Zoom`, `Assistant`, `Move`, `Crop`, `Deform`, `Straighten`, `Perspective Correction`, `Mesh Warp`, `Selection`, `Freehand Selection`, `Magic Wand`, `Foreground Select`, `Dropper`, `Paint Brush`, `Airbrush`, `Color Smudge`, `Warp Brush`, `Lighten/Darken`, `Dodge/Burn`, `Smudge`, `Soften Brush`, `Sharpen Brush`, `Saturation Up/Down`, `Hue Up/Down`, `Red-eye Removal`, `Clone Brush`, `Heal Brush`, `Scratch Remover`, `Object Remover`, `Color Replacer`, `Eraser`, `Flood Fill`, `Picture Tube`, `Text`, `Line`, `Preset Shape`, `Object Selector`, `Pen`
